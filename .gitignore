### macOS
# Finder metadata
.DS_Store

# Thumbnails
._*

# Custom folder icons
Icon

# Volume root files
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
### trailmix
# dist/ is generated from src/ but committed — it's the publishable plugin
# (marketplace installs read it directly from this repo). Run `npm run build`
# after any src/ or build/maps/ change; `npm run verify` fails if it's stale.

# local Claude Code permission grants — per-machine, never shared
.claude/settings.local.json
